---
// 「關於」段(原 handoff 的「誰做的」#founder)。
//
// 2026-08-25 依使用者要求大幅簡化並移到頁尾前:
// - 刪掉學校 logo(georgia-tech.png 是印著 ® 的運動代表隊商標,非學術標誌;
//   ucla.png 是 4K 圖只顯示 46px。handoff README:379 也把校徽列為上線前待確認,
//   並指出保守作法是只留文字學歷)
// - 刪掉引言卡、排名數字(U.S. News 每年會過期)、三張經歷卡
// - 原型把這段放在第二段當作最強說服點(README 1.2),使用者選擇改為低調呈現
//
// 保留的是三邊經歷這個事實本身 —— 那是產品可信度的來源,不是自我介紹。
import { SITE } from '../../config';
---

<section id="founder" class="section section--ground about">
  <div class="wrap about-inner">
    <div class="about-head">
      <div class="kicker">關於</div>
      <h2 class="about-h2">監造、機關、系統，三邊我都待過</h2>
    </div>

    <div class="about-body">
      <p class="about-desc">
        這套系統不是外包做的。從資料模型到 AI 邊界，都是我自己決定的——因為這三種立場我都親自坐過：在<strong
          class="about-em">台灣世曦工程顧問</strong
        >做過監造，查驗、送審、估驗覆核都自己來；在<strong class="about-em">公務機關 4 年</strong
        >走過簽辦、審查、發包到驗收；現在在 AI Agent 新創做產品經理，這套系統從資料模型到 AI 邊界都自己決定。
      </p>
      <p class="about-meta">
        {SITE.founderName} · {SITE.founderTitle}<br />
        UCLA MBA · Georgia Tech 資訊工程碩士 · 交通大學、中央大學 土木工程
      </p>
      <a class="about-mail" href={`mailto:${SITE.contactEmail}`}>
        <span class="msy about-mail-icon" aria-hidden="true">mail</span>{SITE.contactEmail}
      </a>
    </div>
  </div>
</section>

<style>
  .about-inner {
    display: grid;
    grid-template-columns: 300px 1fr;
    gap: 44px;
    align-items: start;
  }
  @media (max-width: 920px) {
    .about-inner {
      grid-template-columns: 1fr;
      gap: 20px;
    }
  }

  .about-h2 {
    font-size: clamp(23px, 2.4vw, 26px);
    line-height: 1.3;
    margin-top: 16px;
  }

  .about-desc {
    font-size: 14px;
    line-height: 1.9;
    color: var(--color-neutral-800);
    max-width: 660px;
  }
  .about-em { font-weight: 500; }

  .about-meta {
    margin-top: 16px;
    font-size: 12.5px;
    line-height: 1.85;
    color: var(--color-neutral-700);
  }

  .about-mail {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    margin-top: 14px;
    font-size: 12.5px;
  }
  .about-mail-icon { font-size: 16px; }

  @media (max-width: 768px) {
    .about-mail { min-height: 44px; }
  }
</style>
