---
// landing「AI 只做草稿」段落。視覺照 handoff「PMIS.ai 官網.dc.html」第 419–453 行 1:1 重建，
// inline style 改寫成下方 scoped class，值未做任何調整；文案逐字沿用。
//
// 外框用 global.css 的 .section / .section--ground / .wrap / .kicker /
// .section-h2 / .section-desc / .split / .num / .msy，其餘卡片樣式在本檔 scoped。
//
// 四條紅線是產品既定原則，文字不得改動。
const redLines = [
  {
    no: '紅線 01',
    title: 'AI 只產生草稿',
    desc: '核定、判定、結案、驗收、凍結——這些動作不在工具白名單裡。草稿寫入待覆核清單，由人接受或拒絕。',
  },
  {
    no: '紅線 02',
    title: '數字由確定性引擎算',
    // 2026-08-25:原文「也不編條號」與 parse-contract/extract-requirements 的實際輸出不符
    // (source_clause 由模型產生且未經程式驗證,只有 quotation 有逐字比對)。
    // 收斂成守得住的說法。
    desc: '金額、期限與合格判定由程式或資料庫 Trigger 執行。AI 可以複述金額，但不自己乘除；審查要點的依據只能引本案已解析的履約需求，不編契約條號。',
  },
  {
    no: '紅線 03',
    title: '每個動作都留痕',
    // 2026-08-25:原文後半宣稱「AI 建議採納率」是可對外交代的指標,
    // 但產品並未實作該指標(agent_actions 有 status/resolved_at 足以計算,
    // 但無任何程式產出此數字)。移除不實聲稱,其餘一字未動。
    desc: '角色、種類、目標、理由、佐證與人的覆核結果都記下來。這同時是稽核軌跡。',
  },
  {
    no: '紅線 04',
    title: '每個 AI 功能可獨立開關',
    desc: '閘門在伺服器端，不是把前端按鈕藏起來。每次呼叫都記錄功能、使用者、專案與成本；出事能單獨關掉一個功能。',
  },
];
---

<section class="section section--ground">
  <div class="wrap">
    <div class="rl-intro">
      <div class="kicker">誰做哪一段</div>
      <h2 class="section-h2">AI 起草，程式算數，人核定</h2>
      <p class="section-desc">
        這條分工不是為了給 AI 設限，是為了讓草稿能用。金額與期限由程式算，所以草稿裡的數字你不必重算一遍；引註由程式驗，所以你點一下就跳到原文那一頁；核定、判定、結案與驗收不在 Agent 的工具清單裡，所以你簽的名還是你的。
      </p>
    </div>

    {/* 三段分工帶:只有 AI 那欄用 AI 面板色,沿用 /security 頁「第三步是唯一 AI 色」的規則 */}
    <div class="rl-lanes">
      <div class="rl-lane rl-lane--ai">
        <div class="rl-lane-title">AI 起草</div>
        <p class="rl-lane-text">讀文件、看照片、彙整資料、寫草稿與建議。產出物一律進待覆核清單，不進業務資料表。</p>
      </div>
      <div class="rl-lane">
        <div class="rl-lane-title">確定性引擎算數</div>
        <p class="rl-lane-text">金額、數量、期限、合格判定與跨文件對帳。不經過模型。</p>
      </div>
      <div class="rl-lane">
        <div class="rl-lane-title">人核定</div>
        <p class="rl-lane-text">接受、修改或拒絕。判定與簽名由本人執行，每一次都寫進不可竄改的稽核軌跡。</p>
      </div>
    </div>

    <div class="split rl-grid">
      {
        redLines.map((line) => (
          <div class="rl-card">
            <div class="num rl-no">{line.no}</div>
            <h3 class="rl-title">{line.title}</h3>
            <p class="rl-desc">{line.desc}</p>
          </div>
        ))
      }
    </div>

    <div class="rl-note">
      <span class="msy rl-note-icon" aria-hidden="true">auto_awesome</span>
      <p class="rl-note-text">
        AI 抽出的每一條履約要求，都附著它引用的那段原文與頁碼。系統會把這段文字正規化後，拿去跟那一頁入庫的文字逐字比對：對得上才標為已驗證；出現在別頁，判為未驗證，不會替它改頁碼；引文短於六個字，一律不算通過。這件事不是 AI 自評，是程式做的——而且資料庫還多擋一層：任何人，包括有審查權的人，直接寫入「已驗證」旗標都會被拒絕。
      </p>
    </div>
  </div>
</section>

<style>
  /* 三段分工帶:AI / 引擎 / 人 */
  .rl-lanes {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
    margin-top: 32px;
  }
  @media (max-width: 760px) {
    .rl-lanes { grid-template-columns: 1fr; }
  }
  .rl-lane {
    border: 1px solid var(--g-line);
    border-radius: 12px;
    padding: 18px;
    background: #fff;
  }
  .rl-lane--ai {
    background: var(--ai-tint);
    border-color: transparent;
  }
  .rl-lane-title {
    font-size: 14.5px;
    font-family: var(--font-heading);
    font-weight: 500;
  }
  .rl-lane--ai .rl-lane-title { color: var(--ai-text); }
  .rl-lane-text {
    margin-top: 8px;
    font-size: 12.5px;
    line-height: 1.8;
    color: var(--color-neutral-700);
  }
  .rl-lane--ai .rl-lane-text { color: var(--ai-text); }

  .rl-intro {
    max-width: 660px;
  }

  /* .split 預設 gap 56px、920px 以下 34px；這四張卡照原型固定 20px（原型是
     class="split" style="gap:20px"），所以兩個斷點都要壓成 20px。 */
  .rl-grid {
    margin-top: 40px;
    gap: 20px;
  }
  @media (max-width: 920px) {
    .rl-grid {
      gap: 20px;
    }
  }

  .rl-card {
    border: 1px solid var(--g-line);
    border-radius: 12px;
    padding: 24px;
    background: #fff;
  }

  /* 標號吃 .num 的 tabular-nums */
  .rl-no {
    font-family: var(--font-display);
    font-size: 13px;
    color: var(--color-accent-700);
    font-weight: 500;
  }

  .rl-title {
    font-size: 19px;
    font-family: var(--font-heading);
    font-weight: 500;
    margin-top: 10px;
  }

  .rl-desc {
    margin-top: 10px;
    font-size: 13.5px;
    line-height: 1.85;
    color: var(--color-neutral-800);
  }

  /* AI 補充列：--ai-tint 底、--ai 圖示、--ai-text 文字 */
  .rl-note {
    margin-top: 20px;
    display: flex;
    align-items: flex-start;
    gap: 12px;
    padding: 16px 18px;
    background: var(--ai-tint);
    border-radius: 12px;
  }

  .rl-note-icon {
    font-size: 20px;
    color: var(--ai);
    flex: none;
  }

  .rl-note-text {
    font-size: 13.5px;
    line-height: 1.85;
    color: var(--ai-text);
  }
</style>
